name: 'snowplow'
version: '0.9.0'

require-dbt-version: ">=0.16.0"

config-version: 2

target-path: "target"
clean-targets: ["target", "dbt_modules", "dbt_packages"]
macro-paths: ["macros"]
log-path: "logs"
